<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Panel Bota</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        /* Style dla strony zarządzania (bez zmian) */
        .management-table { width: 100%; border-collapse: collapse; margin-top: 1.5rem; }
        .management-table th, .management-table td { padding: 0.8rem 1rem; border: 1px solid var(--border-color); text-align: left; vertical-align: middle; }
        .management-table th { background-color: var(--bg-color); color: var(--header-color); }
        .form-inline { display: flex; gap: 1rem; align-items: center; flex-wrap: wrap; }
        .form-inline input { background-color: var(--bg-color); border: 1px solid var(--border-color); color: var(--text-color); padding: 0.8rem; border-radius: 5px; flex-grow: 1; }
        .btn { padding: 0.8rem 1.5rem; border: none; border-radius: 5px; cursor: pointer; font-weight: 700; text-decoration: none; display: inline-block; text-align: center; }
        .btn-primary { background-color: var(--green); color: var(--bg-color); }
        .btn-danger { background-color: var(--red); color: var(--bg-color); font-size: 0.8rem; padding: 0.4rem 0.8rem; }
        .btn-secondary { background-color: var(--border-color); color: var(--text-color); }
    </style>
</head>
<body>
    <header class="header">
        <h1><%= title %></h1>
    </header>

    <main class="dashboard-grid">
        <div class="card" style="grid-column: 1 / -1;">
            <h2>Dodaj Nowy Wpis</h2>
            <form action="<%= addAction %>" method="POST" class="form-inline">
                <% if (headers.server) { %>
                    <input type="text" name="serverId" placeholder="ID Serwera" required style="flex-basis: 100%;">
                <% } %>
                <input type="text" name="id" placeholder="<%= placeholders.id %>" required>
                <input type="text" name="name" placeholder="<%= placeholders.name %>" required>
                <button type="submit" class="btn btn-primary">Dodaj</button>
            </form>
        </div>

        <div class="card" style="grid-column: 1 / -1;">
            <h2>Istniejące Wpisy</h2>
            <div style="overflow-x:auto;">
                <table class="management-table">
                    <thead>
                        <tr>
                            <th><%= headers.id %></th>
                            <th><%= headers.name %></th>
                            <% if (headers.server) { %><th><%= headers.server %></th><% } %>
                            <th>Akcje</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (items.length === 0) { %>
                            <tr><td colspan="4" style="text-align: center;">Brak wpisów.</td></tr>
                        <% } else { %>
                            <% items.forEach(item => { %>
                                <tr>
                                    <td><code><%= item.id %></code></td>
                                    <td><%= item.name %></td>
                                    <% if (headers.server) { %><td><code><%= item.serverId || 'B/D' %></code></td><% } %>
                                    <td>
                                        <form action="<%= deleteAction %>" method="POST" style="margin: 0;">
                                            <input type="hidden" name="id" value="<%= item.id %>">
                                            <button type="submit" class="btn btn-danger">Usuń</button>
                                        </form>
                                    </td>
                                </tr>
                            <% }) %>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
        <div style="grid-column: 1 / -1; text-align: center; margin-top: 1rem;">
             <a href="/" class="btn btn-secondary">Wróć do Głównego Panelu</a>
        </div>
    </main>
</body>
</html>